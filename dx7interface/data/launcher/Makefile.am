launcher_DATA = \
	${PACKAGE_NAME}.desktop

launcherdir = \
	$(datadir)/applications

all-local:
	sed -i 's|$${datarootdir}|$(datarootdir)|g' ${PACKAGE_NAME}.desktop
## File created by the gnome-build tools
