#Questions

### DOC
doc d'utilisation* (DEBIAN)(WINDOWS) utilisation generale( cmd line, ...)
page html
normalisation fichier ui (noms d'objets, l'element parent d'un module->box_main)
normalisation gemod (notebook_main)(menu_add_module, menu_del_module); ->attach_signal 

## Dossier DX7
clean dossier ( sous repertoire: utilitaires(bouclemidi), instruments(.ins), banque de sons(.syx/.fct), controller(midilearn), ) 

# LANG
traductions

# Fonctionalitées
evenements MIDI (temporisation, ?mode omni?)

sépration des mutes (hexter vs natif)

multiplateforme (midi/interface) :
    libremidi (prefered but not compiled for ucrt64 and does not do virtual midi),
    rtmidi(win done with ucrt64)  
    ??? Juce (license prorietaire), drumkit (sequenceur midi)
    nettoyer le dossier ucrt64 lors du packging
	
# MIDI
* reception/envoi en midi ( format TF1(envoi firchier tf1) )
* ecriture fichier des extra parameters( compatible tf1 )
listen sysex message (Windows)

# Interface
rescale la courbe de volume en %
? Select midi device from interface ?
moove(ctrl+c/ctrl+x/drag and drop) gestion click appuyer pour déplacer un sons
refonte interface( size/expand des elements)
* son par defaut (son init) au demrrage et lors d'extentnion de la banque
randomize ( par group(lfo,pitch,...), Valeurs min max par type ( volume min20, en fonction dela frequence, relatif a d'autres ...) )
	- que ce soit audible
	- "musicale"
	- 

#Theme
* test css valeur pour themes clair/fonce.

### CODE
* factorisation ( ?receive bank? )
* genericité / moove to other class (synth,...) ?midi learn,compare?
synth generique (voir avec marc)
gestion d'exceptions
filtre des variables utilisateurs ??? getops ?
evenement standard (erreur ???)
fenetre secondaires (option, module) gxinterface
? LOG / Console integré ? ( mode windows ou sans terminal)
verifier la récuperations des bon bit du syx(cf tableau.ods)

# ergonomie
drag&drop
tooltype


