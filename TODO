### DOC
doc d'utilisation*
normalisation fichier ui (noms d'objets, l'element parent d'un module->box_main)
normalisation gemod (notebook_main)(menu_add_module, menu_del_module); ->attach_signal 
normalisation gx_module (fonction load,

# LANG
traductions

# effectivité
evenements MIDI (temporisation, CC)
ecriture fichier des extra parameters
reception/envoi en midi (recevie bank, receive sound, send bank(32), plusieurs sons)
multiplateforme (midi/interface) drumkit, Juce ???

# Interface
insert/replace/delete/(moove)
garder fonction parameter quelque soit le son (à cocher)
fenetre secondaires (option, module)
refonte interface( size/expand des elements)
son par defaut (son init)
boutons randomize
options des couleurs
drive dx7interface par control change ( fonction assign controle change to event )

### CODE
gestion d'exceptions
filtre des variables utilisateurs ???
evenement standard (erreur ???)
fenetre secondaires (option, module) gxinterface
verifier la récuperations des bon bit du syx(cf tableau.ods)

# ergonomie
drag&drop
tooltype


